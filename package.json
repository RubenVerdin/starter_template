{
  "name": "porfolio",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "vue-tsc --noEmit && vite build && git add . && git commit -m 'app build' && git push origin main"
  },
  "dependencies": {
    "@emailjs/browser": "^3.6.2",
    "uikit": "3.4.2",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@types/node": "^17.0.31",
    "@vitejs/plugin-vue": "^2.3.1",
    "@types/uikit": "^3.3.0",
    "sass": "^1.51.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.7",
    "vue-tsc": "^0.34.7"
  }
}
